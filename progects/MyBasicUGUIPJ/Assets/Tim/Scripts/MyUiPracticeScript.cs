using UnityEngine;
using UnityEngine.UI;
public class MyUiPracticeScript : MonoBehaviour
{
    [SerializeField] private Image mainImage;
    [SerializeField] private Button changeImageButton;
    [SerializeField] private Text imageText;
    [SerializeField] private InputField  inputField;
    [SerializeField] private Sprite spriteCat;
    [SerializeField] private Sprite spriteDog;
    [SerializeField] private Sprite spriteTurtle;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
